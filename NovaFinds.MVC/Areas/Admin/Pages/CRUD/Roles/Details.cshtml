@page
@model NovaFinds.MVC.Areas.Admin.Pages.CRUD.Roles.DetailsModel

@{ ViewData["Title"] = "Role Details"; }

<h1>@ViewData["Title"]</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role!.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role!.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role!.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role!.Name)
        </dd>
    </dl>
</div>
<div>
    <div class="btn-group mt-4" role="group">
        <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@Model.Role!.Id">Edit</a>
        <a class="btn btn-outline-primary" asp-page="./Index">Back to List</a>
    </div>
</div>